doctype
html
    head
        meta(charset="utf8")
        meta(name="site",content="test")
        title=title
    body
        ul
            each user in users
                li(class="user-item")
                    span(class='f1')=user.name
                    span(class='f1')=user.password